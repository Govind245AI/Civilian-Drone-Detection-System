# Civilian-Drone-Detection-System
Civilian Drone Detection System is a Convolutional Neural Network (CNN) trained on thousands of images to classify objects as Airplane, Bird, or Drone. The model was built using TensorFlow/Keras, with data cleaning (removing corrupted images), augmentation, and training on 256x256 RGB images. 
